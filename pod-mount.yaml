apiVersion: v1
kind: Pod
metadata:
  name: nginx-configmap-pod-mount
spec:
  containers:
  - name: nginx1
    image: nginx
    volumeMounts:
    - name: config-volume
      mountPath: /etc/config

  volumes:
  - name: config-volume
    configMap:
      name: app-config
